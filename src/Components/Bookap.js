import React from 'react';
import Button from '@material-ui/core/Button';

export default function DisableElevation9() {
  return (
    <Button variant="contained" color="primary" disableElevation>
      Book another Appointment
    </Button>
  );
}
